effect Console { print : String -> () }

fib : Int -> Int
fib(n) ->
    rec loop(a, b, n) ->
        when n = 0 -> a
        else -> loop(b, a + b, n - 1)
    give loop(0, 1, n)
    
main : Effect ()
main -> Console.print (show (fib 10))